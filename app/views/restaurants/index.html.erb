<!DOCTYPE html>
<html lang="ja">
<head>
<link href="/css/main.css" rel="stylesheet" />
<div class="main">
<title>itswitter</title>
</head>

<body>
  <center><h1>料理、いつくる?</h1></center>
  <center><h1>今空いている順番</h1></center>
  <meta neme="refresh" content="10">
  <% num=1 %>
  <% @rank.each do |ranking| %>
  <tr>
    <% min = ((Time.zone.now - ranking.updated_at).to_i)/60 %>
    <center><h1><%= num  %>位 <%= ranking.name %> <%= ranking.seats_occ %>％ 
	<%= ranking.updated_at.strftime("%m/%d %H:%M:%S") %> <%= min %>分前 </h1><center>
  </tr>
  <% num = num+1 %>
  <% end %>
  <br>
</body>

</div>

</html>
