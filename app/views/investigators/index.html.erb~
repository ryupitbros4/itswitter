<h1>調査ページ</h1>

  <% @jams.each do |jam| %>
    <tr>
    <h2><%= jam.store %></h2>
      <%= form_tag(dbupdate_path(jam.id)) %>
      店内外の人数:<%= text_field_tag :congestion %>
      <%= submit_tag '送信' %>
    </tr>
  <% end %>
