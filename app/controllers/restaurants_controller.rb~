class RestaurantsController < ApplicationController

  def index
    @restaurants = Restaurant.all
    @per_array = Array.new

    @restaurants.each do |restaurant|
      per = (restaurant.num_people.to_f/restaurant.num_seats.to_f) * 100
      @per_array.push(per)
    end
  end

end
