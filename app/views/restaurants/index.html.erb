<p>更新情報<iframe src=<%= url_for(restaurants_slide_info_path) %> height="30" width="90%" scrolling="no" align="middle"></iframe></p>
  <div class="jumbotron">
    <center><h1>Suitell</h1>
      <p>飯、待てぬ、食わねば。</p>
      <%= form_tag(restaurants_search_path) do |f| %>
	<div class="input-group">
	  <%= text_field_tag :name,"" ,class: 'form-control', placeholder: 'お店を検索しよう！' %>
          <span class="input-group-btn">
   	    <%= button_tag '検索', class: 'btn btn-default' %>
          </span>
	</div>
      <% end %>
      <p class="text-warning"><%= flash[:warning] %></p>
    </center>
  </div>
  
  
  <center><h1>今、空いてる順</h1></center>
    <table class="table table-hover">
      <thead>
    	<tr>
          <th class="col-md-1">順位</th>
          <th class="col-md-4">店名</th>
	  <th class="col-md-4">混雑度</th>
	  <th class="col-md-3">最新更新時</th>
    	</tr>
      </thead>
      <tbody>
	<% num=1 %>
	<% @rank.each do |ranking| %>
          <tr>
            <th scope="row" class="col-md-1"><%= num %>位</th>
            <td class="col-md-4"><%= link_to ranking.name, "https://www.google.co.jp/search?q=" + ranking.name %></td>
	    <td class="col-md-4"><%= image_tag(@crowded_image[ranking.crowdedness], :size => "32x32") %><%= link_to @how_crowded[ranking.crowdedness], :action => "report", :resname => ranking.name %></td>
<<<<<<< HEAD
	    <!-- 1時間以上経っている場合、"1時間以上前"と表示させる そうでない場合は最新更新時を表示 -->
	    <td class="col-md-3"><% if (Time.zone.now - ranking.updated_at).to_i > 3600 %>1時間以上<% else %><%= sec2h((Time.zone.now - ranking.updated_at).to_i) %><% end %>前</td>
=======
	    <td class="col-md-3"><%= sec2h((Time.zone.now - ranking.updated_at).to_i) %>前</td>
>>>>>>> f15b2aa4006739ea0a46e26c9187cd19d4d77a27
          </tr>
          <% num = num+1 %>
	<% end %>
      </tbody>
    </table>

  <center><h1>新着</h1></center>
    <table class="table table-hover">
      <thead>
        <tr>
          <th class="col-md-1">店名</th>
          <th class="col-md-4">混雑度</th>
          <th class="col-md-3">最新更新時</th>
          <th class="col-md-4">登録日</th>
        </tr>
      </thead>
      <tbody>
	  <% @new_restaurants.each do |nr| %>
        <tr>
          <td class="col-md-1"><%= link_to nr.name, "https://www.google.co.jp/search?q=" + nr.name %></td>
          <td class="col-md-4"><%= image_tag(@crowded_image[nr.crowdedness], :size => "32x32") %><%= link_to @how_crowded[nr.crowdedness], :action => "report", :resname => nr.name %></td>
          <td class="col-md-3"><%= sec2h((Time.zone.now - nr.updated_at).to_i) %>前</td>
          <td class="col-md-4"><%= nr.created_at.strftime("%Y年%m月%d日 %H時%M分") %></td>
        </tr>
      <% end %>
      </tbody>
    </table>
